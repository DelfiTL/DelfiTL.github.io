
class Capturas {

    constructor() {
        
    }

    captura() {
        if (label == 'aplauso') {
            label = '';
            noLoop();
            image(fondo, 0, 0, windowWidth, windowHeight);
        }
    }
}
